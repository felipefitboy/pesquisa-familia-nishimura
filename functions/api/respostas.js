export async function onRequestPost({request,env}){try{const d=await request.json();const required=['frequencia','qualidade','variedade','atendimento','limpeza','custo_beneficio','compraria_sobremesa','recomendacao'];if(required.some(k=>d[k]===undefined||d[k]==='')||!Array.isArray(d.sobremesas)||!d.sobremesas.length)return Response.json({ok:false,error:'Campos obrigatórios ausentes'},{status:400});const q=`INSERT INTO respostas (frequencia,qualidade,variedade,atendimento,limpeza,custo_beneficio,prato_favorito,prato_desejado,compraria_sobremesa,sobremesas,sobremesa_outra,recomendacao,comentario) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)`;await env.DB.prepare(q).bind(d.frequencia,+d.qualidade,+d.variedade,+d.atendimento,+d.limpeza,+d.custo_beneficio,(d.prato_favorito||'').slice(0,300),(d.prato_desejado||'').slice(0,300),d.compraria_sobremesa,JSON.stringify(d.sobremesas),(d.sobremesa_outra||'').slice(0,120),+d.recomendacao,(d.comentario||'').slice(0,600)).run();return Response.json({ok:true})}catch(e){return Response.json({ok:false,error:'Erro interno'},{status:500})}}
